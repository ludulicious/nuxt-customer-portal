---
title: Architecture overview
description: How the thin host, portal core, and feature layers combine into one Customer Portal application.
githubPath: content/2.architecture/1.overview.md
navigation:
  icon: i-lucide-network
---

Customer Portal is a Nuxt application assembled from layers. The architecture keeps shared infrastructure stable while allowing business capabilities to evolve independently.

## Three responsibilities

### The host

The root application is intentionally small. It provides the Nuxt entry point, global configuration, error handling, global assets, and the ordered database migration history. It should not contain feature-specific menus, pages, or server handlers.

### Portal core

`layers/portal-core` is the contract between the host and every feature. It owns:

- the authenticated application shell;
- session and active-organization state;
- authorization helpers and database access;
- the dashboard and feature registry;
- shared types for navigation, modules, widgets, audiences, and policies.

### Feature layers

A feature layer owns a vertical slice of the product. It may contribute pages, components, composables, server routes, translations, shared types, tests, and tables in its own PostgreSQL schema.

```text
Host application
├── portal-core
│   ├── shell and session
│   ├── authorization and database adapters
│   └── feature registry
└── feature layers
    ├── service-requests
    └── timesheets
        ├── timesheets module
        └── invoices module
```

## Runtime composition

Each feature registers a `PortalFeatureDefinition` through a Nuxt plugin. Portal core collects those definitions and derives:

- global navigation;
- module-aware sidebars based on the current route;
- dashboard widgets grouped by area and order;
- role policies used by server authorization.

This means a feature can be installed or removed without adding imports to the shell.

## Data ownership

Portal core and authentication tables live in PostgreSQL’s `public` schema. Each business feature owns a schema derived from its layer name, such as `timesheets` or `service_requests`.

The host still owns one migration history. This preserves deployment ordering while keeping table ownership clear.

Continue with [feature layers](/architecture/layers), [core contracts](/architecture/core-contracts), or [tenancy and security](/architecture/tenancy-and-security).
