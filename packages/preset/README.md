# Nuxt Customer Portal preset

The convenient platform layer for a new portal host. It composes core, the
neutral UI shell, authentication, organizations, and system administration.
Business modules such as service requests and timesheets stay opt-in.
